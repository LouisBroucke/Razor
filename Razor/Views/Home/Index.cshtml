@{
    Layout = "~/Views/Shared/_AndereLayout.cshtml";
}

@model Razor.Models.Persoon
Doorgegeven persoon: @Model.Voornaam @Model.Familienaam

<a asp-action="Privacy" asp-controller="Home">Naar de privacy pagina</a>

@*@{
                    ViewData["Title"] = "Home Page";
                }

    <div class="text-center">
        <h1 class="display-4">Welcome</h1>
        <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
    </div>
    <div>
        <h1>@(1+2*5+0.5)</h1>

        <h2>Countdown</h2>
        @{
            var getal = 10;
            while (getal > 0)
            {
                <p>@getal</p>
                getal--;
        }
    }

        <h2>De for-lus</h2>
        @for (int i = 0; i < 10; i++)
        {
            <p>@i</p>
        }

        <h2>Random numbers</h2>
        @if (new Random().Next(1,11) == 7)
        {
            <text>Het is zeven</text>
            @: Je hebt geluk
        }
        else
        {
            <text>Het is geen zeven</text>
            @: Een andere keer misschien
        }

        <h2>Fibonacci</h2>
        @{
            var a = 0;
    var b = 1;
    var c = 0;

            while (a < 150)
            {
                <p>@a</p>

                c = a + b;
    a = b;
                b = c;

            }
        }
    </div>*@
